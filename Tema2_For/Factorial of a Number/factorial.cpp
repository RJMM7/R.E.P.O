#include <iostream>

using namespace std;

int main () {

    int number;

    // Se le pide al usuario que digite cualquier numero
    cout << "Please type any number: ";
    cin >> number;

    if(number < 0) {
        cout << "The factorial is only for positive numbers";
    } else {
        for(int i = 1; i >= 0; --i) {
            
        }

        cout << number << "! = " << 
    }

    

    return 0;
}